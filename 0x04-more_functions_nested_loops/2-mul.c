#include "main.h"
#include <stdio.h>

/**
 * mul(int a, int b)- a function that multiplies two integers
 * @a: integer
 * @b: integer
 * Return: The result of a * b
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
