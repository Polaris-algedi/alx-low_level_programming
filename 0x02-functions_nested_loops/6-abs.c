#include "main.h"

/**
 * _abs()- Return the absolute value of a integer
 * @n: The character to check
 *
 * Return: 1 if @n is positive, 0 if @n is zero else -1
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else 
		return (-n);
}
