#include "main.h"

/**
 * add()- Adds two integers and returns the result
 * @n: The integer to add
 * @m: The integer to add
 *
 * Return: 1 if @n is positive, 0 if @n is zero else -1
 */
int add(int n, int m)
{
	int r;

	r = n + m;
	return (r);
}
